# Copy to .env and adjust for local dev / deployments
#
# Docker Compose will automatically load .env in the project root.
#
# ⚠️  DEV-ONLY WARNING
# This repository includes "easy" defaults to get Phase 1 running locally.
# Do NOT reuse these values for any real deployment. Create a separate,
# deployment-specific .env and generate strong secrets.

# ---- Postgres ----
# DEV ONLY defaults. Do not reuse for real deployments.
POSTGRES_DB=foodie
POSTGRES_USER=foodie
POSTGRES_PASSWORD=foodie

# ---- API ----
# Used by the API container. In dev compose, points at the db service.
DATABASE_URL=postgresql+psycopg://foodie:foodie@db:5432/foodie

# Demo seed data (DEV ONLY)
# Dev docker-compose.yml runs the demo seed migration by default.
# Set to 0 to disable.
FOODIE_DEMO_SEED=1

# Auth / JWT (DEV ONLY defaults - do not reuse for production)
JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
JWT_ACCESS_EXPIRES_SECONDS=900
JWT_REFRESH_EXPIRES_SECONDS=2592000

# CORS origins for browser-based frontend (dev)
# NOTE: When allow_credentials=true (our default), you MUST NOT use '*'.
CORS_ORIGINS=http://localhost:5173,http://localhost

# ---- Ports ----
API_PORT=8000
WEB_PORT=5173
NGINX_PORT=8080
